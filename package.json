{
  "name": "zen-chess-fullstack",
  "version": "1.0.0",
  "description": "Unified build and execution scripts for Zen Chess",
  "main": "server/server.js",
  "scripts": {
    "install-server": "npm install --prefix server",
    "install-client": "npm install --prefix client",
    "build-client": "npm run build --prefix client",
    "build": "npm run install-server && npm run install-client && npm run build-client",
    "render-build": "npm run build",
    "start": "node server/server.js",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
